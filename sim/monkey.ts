type obj = {
 x: number;
 y: number;
};
const g = 9.8;
const acceleration = (a: number,t: number) => {
  y = at;
  return y;
};
fire: number;
angle: number;
b: obj = {
   y: fire * Math.cos(angle) * t - 1 / 2 * acceleration(g,t) * t;
   x: fire * Math.sin(angle);
};

monkey: obj = {
   y: 1 / 2 * acceleration(g,t) * t;
   x: t;
};

